export interface Money {
    currency: string;
    quantity: number;
    emoji: string;
}

export interface Memory {
    value: number;
    quantity: number;
    emoji: string;
}

export interface student {
    quantity: number;
    emoji: string;
}